package com.kodnest.app.entities;

public enum OrderStatus {
   PENDING,
   SUCCESS,
   FAILED,
   ORDER_PLACED,
   CONFIRMED,
   PACKED,
   SHIPPED,
   OUT_FOR_DELIVERY,
   DELIVERED,
   CANCELLED
}
